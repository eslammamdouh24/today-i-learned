const Source = ({ source }) => {
  return (
    <a
      className="source"
      href={source}
      target="_blank"
      rel="noopener noreferrer"
    >
      (source)
    </a>
  );
};

export default Source;
